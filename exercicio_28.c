#include <stdio.h>

int main() {
    float valor;

    printf("Digite um valor real: ");
    scanf("%f", &valor);

    printf("O valor real digitado foi: %.3f\n", valor);

    return 0;
}
